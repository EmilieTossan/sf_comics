{% extends "admin/base.html.twig" %}
{% block title %}Ajouter un éditeur{% endblock %}

{% block main %}

    {{form_start(editorForm)}}

    {{form_label(editorForm.name, 'Nom de l\'éditeur')}}
    {{form_widget(editorForm.name)}}
    
    {{form_label(editorForm.country, 'Pays d\'origine')}}
    {{form_widget(editorForm.country)}}

    {{form_widget(editorForm.Enregistrer)}}

    {{form_end(editorForm)}}

{% endblock %}