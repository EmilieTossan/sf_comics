{% extends "admin/base.html.twig" %}
{% block title %}Ajouter un éditeur{% endblock %}

{% block main %}

    {{form_start(imageForm)}}

    {{form_label(imageForm.src, 'Image')}}
    {{form_widget(imageForm.src)}}
    
    {{form_label(imageForm.title, 'Titre')}}
    {{form_widget(imageForm.alt)}}

    {{form_label(imageForm.title, 'Alt')}}
    {{form_widget(imageForm.alt)}}

    {{form_label(imageForm.comic, 'Bande dessinée')}}
    {{form_widget(imageForm.comic)}}

    {{form_widget(imageForm.Enregistrer)}}

    {{form_end(imageForm)}}

{% endblock %}