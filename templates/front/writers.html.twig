{% extends "admin/base.html.twig" %}
{% block title %}Découvrez la liste des auteurs{% endblock %}

{% block main %}
    <section style="display: flex; justify-content: center; margin-top: 50vh; transform: translateY(-50%)">

        <table style="border-spacing: 5px;">

            <td>
                <tr><h1 style="margin-top: auto; margin-bottom: auto; width:220px">Les auteurs</h1></tr>
            </td>
            <td>
                <tr><hr></tr>
            </td>
            <td>
                <tr>
                    <div style="text-align: center; width:500px">
                        {% for writer in writers %}
                            <p style="margin: 30px"><a href="{{ path('show_writer', {'id' : writer.id }) }}" style="outline: none; text-decoration: none; color: #000; font-size: 16pt">{{ writer.name }} <span style="color: grey"> - {{ writer.country }}</span></a></p>
                            <hr>
                        {% endfor %}
                    </div>
                </tr>
            </td>

        </table>
        
    </section>
{% endblock %}